id: strict-equality-js
language: JavaScript
message: "Use === instead of =="
severity: warning
note: |
  == performs type coercion which leads to unexpected comparisons.
  Use === for strict equality checks.
rule:
  pattern: $X == $Y
fix: $X === $Y
